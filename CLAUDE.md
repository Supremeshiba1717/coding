# CLAUDE.md

Read this first if you're an AI (Claude, Codex, Copilot, Cursor, whatever) working in this repo.

## What this repo is

A general-purpose repo for coding. It holds lots of **unrelated projects**: whatever the owner wants built that day. No single app, stack, or theme.

## Rules

1. **Put each project in its own folder** at the repo root, e.g. `snake-game/`, `discord-bot/`. Don't mix projects together or dump files at the root.
2. **Give each project a short `README.md`**: what it is, how to run it, and its status.
3. **Log every project and idea in the index below.** That includes ideas that were only talked about and never built. This file is the repo's memory. Nothing carries over between sessions unless it's written here and pushed.
4. **Keep dependencies local to each project**, like its own `package.json` or `requirements.txt`. Don't add repo-wide tooling unless asked.
5. **Commit and push when you're done.** Cloud sessions are ephemeral, so anything left unpushed is lost.

## How to work with the owner

- Be direct. If something is wrong, broken, or a bad idea, say so plainly. Don't sugarcoat it.
- Keep it chill and casual.
- **The owner's latest instruction wins.** If they override an earlier rule, including one in this file, follow the override and update this file to match.
- **Auto-push after the go-ahead.** Once the owner approves something you coded, commit and push it right away. Don't ask "want me to push?" again.
- **ADHD-friendly responses.** Use the `/anthropic-skills:i-have-adhd` style every session:
  - Put the next action first.
  - Number multi-step tasks.
  - Restate where things stand in one line.
  - Don't open with preamble or close with pleasantries.
  - Keep lists to 5 items or fewer.
  - Keep the bluntness.

  In Claude Code, invoke that skill at the start of each session.

## Project index

Keep this table updated. Add a row whenever a project is started or an idea is brought up.

| Project | Folder | Status | Description |
|---------|--------|--------|-------------|
| _(none yet)_ | | | |

Status values: `idea`, `in progress`, `working`, `abandoned`.

## Ideas backlog

Ideas mentioned but not started yet:

- _(none yet)_

## Misc

- `maincode`: an empty file left over from the initial commit. It isn't a project. Delete it once the owner says so.
